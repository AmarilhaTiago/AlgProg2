#include <stdio.h>

int main(){
    int x, y;

    printf("Digite os valores dos dois inteiros:\n");
    scanf("%d", &x);
    scanf("%d", &y);

    printf("A soma dos numeros resultou em: %d", x + y);

    return 0;
}
